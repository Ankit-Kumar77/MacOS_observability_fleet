---
# Monitored Mac environment overrides.
#
# The collector version, checksum, install paths, collection interval and
# macOS filesystem exclusions are role defaults in
# roles/observability_agent/defaults/main.yml. Override them here only when
# this environment genuinely requires something different, for example:
#
#   otel_collection_interval: 30s
